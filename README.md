# Emotion-Detection-In-News-Headline
A machine learning–based web application that classifies emotions in news headlines using TF-IDF feature extraction and a Multinomial Naive Bayes model. The trained model is deployed with a Flask backend and an HTML/CSS frontend for real-time emotion prediction.
